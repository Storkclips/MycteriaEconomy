package me.spiderdeluxe.mycteriaeconomy.inventories;

import org.bukkit.inventory.Inventory;
import org.bukkit.inventory.InventoryHolder;

/**
 * Inventory holder to the currency GUI, where all currency items will be available in order for the player to get
 * them
 */

public class CurrencyHolder implements InventoryHolder {

    @Override
    public Inventory getInventory() {
        return null;
    }
}
